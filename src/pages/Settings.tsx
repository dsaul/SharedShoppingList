import React from 'react';
import style from './Settings.module.css';
import { Routes, Route, Link } from "react-router-dom";

interface ISettingsProps {
	
};

export default (props: ISettingsProps) => {
	return (
		<div>Settings</div>
	);
}
